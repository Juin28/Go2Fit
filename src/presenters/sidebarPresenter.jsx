import { observer } from "mobx-react-lite"
import { SidebarView } from "../views/sidebarView"

export const Sidebar = observer(function SummaryRender(props) {
    return (
        <SidebarView/>
    )
})
