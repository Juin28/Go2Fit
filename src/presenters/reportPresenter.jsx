import { observer } from "mobx-react-lite"
import { ReportView } from "../views/reportView"

export const Report = observer(function SummaryRender(props) {
    return (
        <ReportView/>
    )
})
